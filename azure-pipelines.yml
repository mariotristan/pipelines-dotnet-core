pr:
  branches:
    include:
      - master

pool:
  vmImage: 'ubuntu-latest'

steps:
- script: echo "This pipeline ran because a PR was created or updated targeting master."
  displayName: 'PR Validation Step'
